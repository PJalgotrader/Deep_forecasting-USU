[project]
name = "uv-simple-test"
version = "0.1.0"
description = "A tiny uv installation test for DATA 5630 students"
requires-python = ">=3.11,<3.12"
dependencies = [
    "rich>=15,<16",
]

[tool.uv]
package = false
